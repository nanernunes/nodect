{
  "type": "module",
  "name": "nodect",
  "version": "1.0.2",
  "main": "index.mjs",
  "license": "MIT",
  "scripts": {
    "test": "yarn node --no-warnings --experimental-vm-modules $(yarn bin jest) --config=jest.config.mjs"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.1",
    "prettier": "2.7.1"
  }
}
